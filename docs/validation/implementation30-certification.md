# Implementation 30 Certification

Generated at: 2026-07-15T21:39:15.472Z

Status: pass

| Evidence ID | Area | Status | Checks |
| --- | --- | --- | --- |
| IMPLEMENTATION30-001-mpesa-callback-integrity | M-Pesa callback trust, verification, and allocation safety | pass | pass: STK callbacks require signature or provider verification before payment state changes; pass: M-Pesa callback trust service exposes edge-signed, direct Daraja, and manual-review modes; pass: M-Pesa callback channels store hashed channel secrets, source CIDRs, and per-channel verification flags behind tenant RLS; pass: M-Pesa callback channel service builds channel-secret STK and C2B callback URLs and hashes secret refs; pass: M-Pesa callback channel secrets rotate with hashed refs and an overlap window for in-flight callbacks; pass: STK callbacks expose a channel-id and secret-ref route before normal callback processing; pass: C2B validation and confirmation callbacks expose channel-id and secret-ref routes; pass: M-Pesa transaction-status service queries Daraja STK status before provider trust is granted; pass: M-Pesa transaction-status service queries Daraja C2B transaction status before direct Paybill allocation; pass: M-Pesa verification worker grants provider trust and releases payment processing only after Daraja status succeeds; pass: M-Pesa verification worker classifies C2B payments without posting ledger jobs before accountant allocation; pass: Payments worker queue has a durable M-Pesa verification job separate from ledger processing; pass: M-Pesa verification jobs persist provider status attempts and encrypted provider responses behind tenant RLS; pass: C2B payments move through explicit unverified, verification-requested, verified-matched, and verified-unmatched states; pass: Callback logs persist provider verification status separately from edge signature status; pass: M-Pesa verification repository creates STK and C2B verification jobs and encrypts provider responses; pass: Unsigned direct STK callbacks enqueue provider verification instead of payment ledger processing; pass: M-Pesa duplicate identifiers are guarded by checkout, receipt, C2B TransID, and channel uniqueness; pass: Direct Daraja mode can store unsigned callbacks as unverified work without bypassing later verification; pass: Direct Daraja C2B callbacks can be accepted without custom edge HMAC headers; pass: Payments tests cover callback-channel schema, high-entropy URL generation, and channel routes; pass: Payments tests cover M-Pesa verification-job schema, direct callback verification enqueue, duplicate guards, and Daraja status query behavior; pass: Payments tests prove direct Daraja ingress and edge-signed rejection behavior; pass: C2B confirmations are held for provider verification before allocation; pass: Matched C2B confirmations create verification jobs before accountant allocation can post; pass: M-Pesa operational payloads are redacted before normal storage; pass: Raw M-Pesa payloads are encrypted in a vault table with operational hash/ref fields; pass: Raw M-Pesa payload retrieval requires elevated support permission, ticket id, reason, time limit, and audit log; pass: Support payload exports use redacted vault payloads and never decrypt raw callback bodies; pass: M-Pesa workers read full payloads from the encrypted vault instead of redacted operational JSON; pass: STK callback logs store redacted operational payload JSON; pass: C2B callback records store redacted operational payload JSON; pass: C2B operational rows store masked phone and payer-name values by default; pass: C2B API responses and reconciliation payloads are redacted even for legacy rows with raw values |
| IMPLEMENTATION30-002-canonical-mpesa-config | Canonical school-owned M-Pesa configuration | pass | pass: Daraja setup mirrors credentials into tenant finance M-Pesa config; pass: Daraja setup rejects non-HTTPS callback URLs before storing credentials; pass: Integrations module wires tenant finance as the M-Pesa source of truth; pass: Tenant finance schema has canonical M-Pesa config and payment-channel tables; pass: Tenant finance schema rejects duplicate active M-Pesa configs for the same tenant environment and channel; pass: Tenant finance schema stores masked M-Pesa config change audit evidence behind forced RLS; pass: Tenant finance records M-Pesa config changes with masked old/new secret values; pass: Tenant finance rotates M-Pesa credentials and callback secrets without raw callback secret storage; pass: Tenant finance API exposes credential rotation to billing updaters; pass: Tenant finance validates M-Pesa go-live readiness before production activation; pass: Tenant finance summary exposes principal/accountant M-Pesa setup state; pass: Tenant finance API exposes go-live validation to finance readers |
| IMPLEMENTATION30-003-mpesa-reconciliation-finance-close | M-Pesa reconciliation, finance close, and accountant review controls | pass | pass: Payments schema stores reconciliation batches, discrepancy states, finance close periods, and dual approval requests behind tenant RLS; pass: M-Pesa reconciliation service runs daily and date-range reports, persists batches, and classifies mismatch states; pass: M-Pesa accountant review queue redacts evidence and reversal/write-off/move/mismatch actions require two distinct approvers before resolution; pass: Payments API exposes accountant-readable date-range reconciliation and restricted daily processor routes; pass: Payments API exposes accountant review queue and finance approval routes; pass: Bursar/accountant UI exposes unmatched M-Pesa Paybill deposits and reconciles API envelope responses; pass: Payments tests cover reconciliation schema, missing callbacks, duplicates, amount mismatch, wrong references, late provider status, daily processor, and range reports; pass: Payments tests cover accountant review redaction, dual approval reversal, and review routes; pass: Design tests cover reconciling unmatched M-Pesa Paybill deposits from live API envelope responses |
| IMPLEMENTATION30-004-production-config-and-ci | Production config, callback edge limits, and CI signoff | pass | pass: Startup validation requires tenant/report-card signing secrets and rejects insecure production defaults; pass: Startup validation enforces production TLS/SSL, trusted proxy CIDRs, secure cookies, RLS audit, payload vaulting, object storage, and strong PII/KMS config; pass: Env validation tests cover object storage, proxy CIDRs, secure cookies, RLS audit, M-Pesa vaulting, encrypted transports, and KMS key validation; pass: Runtime configuration exposes production hardening flags to database, Redis, auth cookie, KMS, proxy, and M-Pesa payload-vault consumers; pass: PostgreSQL pool honors explicit DATABASE_SSL or sslmode=require for encrypted production transport; pass: Redis and Bull queue connections honor explicit REDIS_TLS_ENABLED as well as rediss URLs; pass: Redis options tests prove explicit TLS flags enable encrypted Redis transport; pass: Startup validation rejects unsupported M-Pesa callback trust modes; pass: Startup validation requires the Daraja transaction-status security credential in production; pass: Runtime configuration exposes the Daraja transaction-status security credential used by verification workers; pass: NGINX applies the M-Pesa rate-limit bucket to STK callbacks; pass: NGINX applies the M-Pesa rate-limit bucket to C2B validation and confirmation callbacks; pass: Default env template includes only a placeholder tenant-header signing secret; pass: Production env template includes only a placeholder tenant-header signing secret; pass: Default env template includes only a placeholder report-card download signing secret; pass: Production env template includes only a placeholder report-card download signing secret; pass: Default env template documents the M-Pesa callback trust mode; pass: Production env template defaults to edge-signed M-Pesa callbacks; pass: Default env template documents the Daraja transaction-status security credential placeholder; pass: Production env template documents the Daraja transaction-status security credential placeholder; pass: Vercel env template requires report-card signing secret injection without checked-in material; pass: Vercel env template defaults to edge-signed M-Pesa callbacks; pass: Vercel env template requires transaction-status security credential injection without checked-in material; pass: Vercel env template does not retain pulled OIDC token material; pass: Secret rotation runbook covers M-Pesa and platform secrets without printing sensitive values; pass: Production operability CI runs Implementation 30 certification and uploads its evidence |
| IMPLEMENTATION30-005-production-observability | Production observability, alerts, runbooks, and synthetic checks | pass | pass: Production observability catalog covers Kenyan school operating dashboards, alerts, runbooks, and synthetic checks; pass: Observability tests prove the production catalog validates dashboards, alerts, runbooks, and synthetics; pass: Synthetic journey monitor probes login, tenant module switching, M-Pesa readiness, parent report-card download, and principal dashboard load; pass: Synthetic journey tests assert the critical Kenyan school probes are registered; pass: M-Pesa callback incident runbook covers callback failures and provider verification backlog; pass: M-Pesa reconciliation runbook covers accountant review and unmatched deposits; pass: Report-card incident runbook covers stuck generation queues; pass: Tenant isolation incident runbook covers RLS and cross-tenant containment; pass: Database saturation runbook covers PgBouncer and connection pool response; pass: SMS campaign failure runbook covers provider delivery response |
| IMPLEMENTATION30-006-backup-restore-and-lifecycle | Backups, restore evidence, and tenant offboarding lifecycle | pass | pass: Backup restore runbook defines scheduled production evidence and the DR command artifact; pass: Backup restore drill covers full schema, tenant-scoped, point-in-time, checksum, and object-storage metadata restore; pass: Backup restore evidence requires encrypted database and object-storage restore proof with RTO/RPO; pass: Platform service exports contract offboarding manifests and anonymizes tenant shells with lifecycle audit events and retention policy; pass: Platform controller exposes tenant offboarding export and legal anonymization routes; pass: Platform tests cover tenant offboarding manifests and legal anonymization metadata; pass: Production operability CI runs the backup restore drill and uploads sanitized evidence |
| IMPLEMENTATION30-007-scale-isolation-and-rate-limits | Scale budgets, tenant isolation audit, and provider callback rate limits | pass | pass: Implementation 30 load profile models 1000+ schools, report-card generation, and M-Pesa reconciliation budgets; pass: Generated load-profile artifact records passing P95/query/queue budget evidence; pass: Tenant isolation audit covers critical RLS and statically scans tenant tables for forced row level security; pass: Nest rate-limit service treats C2B validation and confirmation aliases as M-Pesa provider callback traffic; pass: Security tests cover M-Pesa callback and C2B alias rate-limit buckets; pass: Main test script runs the Redis TLS runtime test |
| IMPLEMENTATION30-008-pilot-rollout-gate | Implementation 30 pilot rollout evidence gate | pass | pass: Rollout gate blocks unevidenced live phases and enforces zero-incident phase exits; pass: Rollout gate tests cover blocked live phases, complete zero-incident evidence, failed payment safety, and secret-safe markdown; pass: Package scripts expose the rollout gate and main test suite runs its tests; pass: Production operability CI runs and uploads the Implementation 30 rollout gate artifact |
| IMPLEMENTATION30-009-exams-mark-integrity | Exams grading, mark integrity, and post-publish correction controls | pass | pass: Exams schema stores grading policies, assessment components, mark versions, report-card batches, artifacts, and publish workflow states behind tenant RLS; pass: Exams service validates mark entry against assessment maximum score before persistence; pass: Exams service enforces configured grade boundaries and supports preview-before-commit bulk mark upload with audit; pass: Exams API exposes bulk mark template and preview/commit upload endpoints under mark-entry permission; pass: Exams repository resolves grade-boundary coverage and overlapping matches for mark validation; pass: Post-publish mark corrections require dual approval, create mark versions, and flag report cards for regeneration; pass: Exams repository persists mark versions and marks published report-card snapshots for regeneration; pass: Exams tests cover grading schema, maximum-score enforcement, grade boundaries, bulk upload, dual approval, and regeneration after published corrections |
| IMPLEMENTATION30-010-exams-report-cards | Backend-generated and immutable report cards | pass | pass: Exams service generates report-card payloads from backend data; pass: Exams API exposes a generation endpoint protected by exams approval; pass: Report-card generation service creates tenant-scoped HTML/PDF artifacts with verification codes and batch status; pass: Report-card template service renders school, learner, comments, totals, and next-term fields for HTML/PDF output; pass: Exams repository persists report-card artifacts, generation batches, and tenant-scoped verification lookup; pass: Exams API exposes regeneration, class batch, batch status, and printed verification endpoints; pass: Published report cards are not overwritten by conflict updates; pass: Report cards are built from student, exam, subject, mark, and grade data; pass: Exam mark conflict updates cannot cross series, term, class, or subject scope; pass: Parent report-card downloads are signed, actor-bound, and resolved through guardian links; pass: Exams API exposes parent report-card download endpoints protected by linked-child portal permission; pass: Report-card parent access joins through active student guardian links; pass: Tests prove withdrawn report cards cannot be downloaded by linked parents; pass: Exams tests cover HTML/PDF artifact generation, class batch progress, and printed verification lookup |
| IMPLEMENTATION30-011-exams-frontend-live | Exams frontend live API integration | pass | pass: Next.js exams API proxy forwards GET, POST, and PATCH requests to the school API exams routes; pass: Exams live client fetches mark sheets, writes marks, previews bulk uploads, locks/corrects sheets, generates batches, publishes cards, and uses guardian-scoped download paths; pass: Exams module screen renders live mark-sheet operations, generated artifact preview, polling batch progress, and role-scoped teacher/principal actions; pass: Design tests exercise live teacher mark entry, bulk preview, lock, principal generation, batch polling, publish, and disabled-module gating; pass: Live adapter tests map backend mark sheets and report-card artifacts and verify parent downloads are report-card scoped; pass: Backend exams API exposes a teacher-scoped mark-sheet lock endpoint for the live frontend |
| IMPLEMENTATION30-012-tenant-trust-boundary | Tenant header trust boundary | pass | pass: Forwarded tenant headers require an HMAC signature; pass: Tenant trust boundary service resolves signed proxy headers, subdomains, and verified custom domains without trusting browser x-tenant-id; pass: Platform onboarding schema stores tenant domain bindings behind forced RLS; pass: Tenant middleware passes the trusted signature header into tenant resolution; pass: Tenant tests prove unsigned forwarded tenant headers are ignored and custom domains resolve through verified bindings |
| IMPLEMENTATION30-013-child-data-protection | Child-data classification, consent, retention, DPIA, and no-leak controls | pass | pass: Security registry classifies child, health, biometric, payment, guardian, M-Pesa, report-card, consent, retention, and DPIA controls; pass: PII leak scanner detects raw Kenyan phone numbers, admission numbers, and parent or payer names in artifacts; pass: Compliance schema stores DSR, retention, child-DPIA, and breach evidence tables behind forced tenant RLS; pass: Compliance service runs audited DSR identity, approval, completion, and anonymization workflow with SLA countdown; pass: Compliance API exposes protected DSR workflow and breach report export routes; pass: Breach response runbook covers containment, notification, sanitized evidence export, and PII scan proof; pass: PII leak CI scan covers generated logs, snapshots/exports, API responses, and frontend fixtures; pass: Security and operability CI run the PII leak artifact scan; pass: Security and compliance tests cover classification registry, PII leak scanning, DSR, retention, DPIA, breach evidence, and RLS; pass: Compliance tests verify data-subject workflow, breach export, retention, DPIA, and tenant RLS; pass: PII leak CI tests prove raw artifact leaks fail and masked artifacts pass |
| IMPLEMENTATION30-014-implementation30-verification | Implementation 30 verification artifacts | pass | pass: Implementation 30 production readiness plan exists; pass: Payments tests cover verification-before-allocation and redaction; pass: Exams tests cover generated, immutable, scoped marks, grading integrity, and guardian-scoped report cards; pass: Module-access certification compares enabled modules against registry, permissions, routes, frontend guards, jobs, reports, dashboard widgets, and package expiry behavior; pass: Module-access certification tests fail when backend or frontend module guard evidence is missing; pass: CI runs module-access certification and uploads its evidence artifact |

## Notes

- This certification checks source-level evidence for the Implementation 30 P0 controls implemented in this pass.
- It focuses on M-Pesa callback integrity, canonical tenant payment configuration, reconciliation and finance-close controls, redacted operational payloads, exams grading integrity, generated report cards, child-data protection evidence, production config gates, immutable published report cards, signed tenant-header trust boundaries, and pilot rollout evidence gating.

