"use client";

import { AcademicIntelligenceWorkspace } from '../academic-intelligence-workspace';

/** Retained for existing links; HOD exam responsibilities begin after publication. */
export function MarksModerationWorkspace() {
  return <AcademicIntelligenceWorkspace audience="hod" />;
}
